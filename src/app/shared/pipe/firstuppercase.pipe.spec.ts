import { FirstuppercasePipe } from './firstuppercase.pipe';

describe('FirstuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
